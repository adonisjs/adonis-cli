{{^ts}}
'use strict'

const Schema = use('Schema')
{{/ts}}
{{#ts}}
import Schema = require('@adonisjs/lucid/src/Schema/index');
{{/ts}}

{{#ts}}export = {{/ts}}class {{ name }} extends Schema {
  {{#create}}
  up () {
    this.create('{{ table }}', (table) => {
      table.increments()
      table.timestamps()
    })
  }

  down () {
    this.drop('{{ table }}')
  }
  {{/create}}
  {{^create}}
  up () {
    this.table('{{ table }}', (table) => {
      // alter table
    })
  }

  down () {
    this.table('{{ table }}', (table) => {
      // reverse alternations
    })
  }
  {{/create}}
}

{{^ts}}
module.exports = {{ name }}
{{/ts}}
